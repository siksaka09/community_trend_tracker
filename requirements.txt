# requirements.txt รวม — ใช้ตัวนี้แทนไฟล์เดิมตอน deploy บน Streamlit Community Cloud
# (Cloud จะติดตั้งจาก requirements.txt ไฟล์เดียวที่ root ของ repo เท่านั้น จึงต้องรวมทุกอย่างไว้ที่นี่)
apify-client>=1.7.0
anthropic>=0.40.0
python-dotenv>=1.0.0
pandas>=2.2.0
streamlit>=1.32.0
plotly>=5.20.0
